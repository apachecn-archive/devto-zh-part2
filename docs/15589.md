# Bash 函数

> 原文:[https://dev.to/macnux/bash-functions-5724](https://dev.to/macnux/bash-functions-5724)

在讨论 bash 函数之前，我们先讨论一下这种情况。在编写 bash 脚本时，您会发现自己在多个地方使用了相同的代码。

如果厌倦了在 bash 脚本中一次又一次地编写相同的代码行，那么最好只编写一次代码块，并在 bash 脚本中的任何位置调用它。

bash shell 允许您用函数做到这一点。

Bash 函数是代码块，您可以在代码中的任何地方重用它们。任何时候你想在你的脚本中使用这个代码块，你只需要输入给它的函数名。

我们将讨论如何创建自己的 bash 函数，以及如何在 shell 脚本中使用它们。

1 创建函数
2 使用函数
3 使用返回命令
4 使用函数输出
5 传递参数
6 处理 Bash 函数中的变量
6.1 全局变量
6.2 局部变量
7 传递数组作为参数
8 递归函数
9 创建库
10 从命令行使用 Bash 函数

[https://likegeeks.com/bash-functions/](https://likegeeks.com/bash-functions/)