# 解释相同时间的多个 setIntervals。

> 原文:[https://dev . to/sathish/explain-multiple-set intervals-with-same-time-4nhj](https://dev.to/sathish/explain-multiple-setintervals-with-same-time-4nhj)

因为 JavaScript 是单线程的，如果我设置两个时间间隔来同时运行两个函数。它会执行两个功能还是只执行一个？

例如，我设置了两个固定间隔，每 30 分钟运行两个功能。

或者它将两个函数排队等待执行？